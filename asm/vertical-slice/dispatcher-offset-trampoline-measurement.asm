; Native ATOM composition. Build flags are immutable entry metadata.

; One-byte semantic dispatcher with page-local trampolines to arbitrary handlers.

%INCLUDE "memory-map.asmi"
%INCLUDE "early-offset-trampoline-driver.asm"
