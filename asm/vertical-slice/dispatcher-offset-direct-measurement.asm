; Native ATOM composition. Build flags are immutable entry metadata.

; Ideal one-byte semantic dispatcher when every handler begins in one page.

%INCLUDE "memory-map.asmi"
%INCLUDE "early-offset-direct-driver.asm"
